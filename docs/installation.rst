============
Installation
============

At the command line::

    $ easy_install django-elasticsearch-dsl

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-elasticsearch-dsl
    $ pip install django-elasticsearch-dsl
